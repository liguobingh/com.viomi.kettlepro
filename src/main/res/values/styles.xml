<resources>

    <style name="popwin_anim_style">
        <item name="android:windowEnterAnimation">@anim/slide_in_right</item>
        <item name="android:windowExitAnimation">@anim/slide_out_right</item>
    </style>

    <declare-styleable name="EaseSwitchButton">
        <attr name="switchOpenImage" format="reference" />
        <attr name="switchCloseImage" format="reference" />
        <attr name="switchStatus">
            <enum name="open" value="0" />
            <enum name="close" value="1" />
        </attr>
    </declare-styleable>

    <style name="CustomCheckboxTheme" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/checkbox_style</item>
    </style>
    <style name="radioButton" parent="@android:style/Widget.CompoundButton.RadioButton">
        <item name="android:drawableRight">@drawable/radiobutton_style</item>
    </style>

    <!--提示框--> 
    <!-- styles.xml on API 14+ devices --> 
    <style name="SampleTheme.Light" parent="android:Theme.Holo.Light.DarkActionBar">   
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="DeviceSeekbarStyle">
        <item name="android:maxHeight">2dp</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@color/colorAccent</item>
        <item name="android:progressDrawable">@drawable/bg_seekbar_progress_bar</item>
        <item name="android:minHeight">2dp</item>
    </style>

</resources>
